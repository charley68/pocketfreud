#!/bin/zsh

# Database configuration for tests
export DB_HOST=localhost
export DB_USER=freud
export DB_PASS=freud2306
export DB_NAME=test_pocketfreud

# Flask configuration
export FLASK_ENV=testing
export FLASK_DEBUG=0
export SECRET_KEY=test_secret_key

# External services configuration
export OPENAI_API_KEY=sk-test-key-not-real
export RECAPTCHA_SECRET_KEY=6Ltest-fake-key
export STRIPE_SECRET_KEY=sk_test_fake
export STRIPE_PUBLISHABLE_KEY=pk_test_fake

# Email configuration
export SMTP_SERVER=localhost
export SMTP_PORT=1025
export SMTP_USERNAME=test
export SMTP_PASSWORD=test
export FROM_EMAIL=test@pocketfreud.ai

# Disable actual API calls during tests
export TEST_MODE=1
